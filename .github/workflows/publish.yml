name: 更新并发布 Reeder RSS

on:
  workflow_dispatch:
  schedule:
    # GitHub cron uses UTC: 12:00/15:00 UTC = 20:00/23:00 Asia/Shanghai.
    - cron: "0 12 * * *"
    - cron: "0 15 * * *"

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: false

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
      PUBLIC_BASE_URL: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}
      TZ: Asia/Shanghai
    steps:
      - name: 读取项目
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: 准备 Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: 安装依赖
        run: python -m pip install -r requirements.txt

      - name: 抓取、评分并生成 RSS
        run: python update_daily.py --publish-dir site --require-api-key

      - name: 输出分阶段统计
        if: always()
        run: |
          if [ -f logs/daily_update_latest.md ]; then
            cat logs/daily_update_latest.md
          fi

      - name: 准备 GitHub Pages
        uses: actions/configure-pages@v5

      - name: 上传 RSS
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

      - name: 发布 GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
